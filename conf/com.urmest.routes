# Authentication Routes
GET   /auth/login     com.urmest.authentication.PasswordAuthenticationController.login(email, password)
GET   /auth/logout    com.urmest.authentication.AuthenticationController.logout()
GET   /auth/status    com.urmest.authentication.AuthenticationController.status()

# User Management Routes
GET   /user/signup    com.urmest.users.UserController.signUp(name, email, password)

# Map static resources from the /public folder to the /assets URL path
GET   /assets/*file   controllers.Assets.at(path="/public", file)